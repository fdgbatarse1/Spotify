import useGetFeaturedPlaylists from '@/hooks/useGetFeaturedPlaylists';

describe('Testing useGetFeaturedPlaylists', () => {
  it('should be defined', () => {
    expect(useGetFeaturedPlaylists).toBeDefined();
  });
});
