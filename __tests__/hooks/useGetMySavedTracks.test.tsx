import useGetMySavedTracks from '@/hooks/useGetMySavedTracks';

describe('Testing useGetMySavedTracks', () => {
  it('should be defined', () => {
    expect(useGetMySavedTracks).toBeDefined();
  });
});
