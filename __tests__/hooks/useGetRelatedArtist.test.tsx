import useGetRelatedArtist from "@/hooks/useGetRelatedArtist";

describe('Testing useGetRelatedArtist', () => {
  it('should be defined', () => {
    expect(useGetRelatedArtist).toBeDefined();
  });
});
