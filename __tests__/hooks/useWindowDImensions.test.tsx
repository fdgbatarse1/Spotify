import useWindowDimensions from '@/hooks/useWindowDimensions';

describe('Testing useWindowDimensions', () => {
  it('should be defined', () => {
    expect(useWindowDimensions).toBeDefined();
  });
});
