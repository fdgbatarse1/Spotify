import useGetMySavedAlbums from '@/hooks/useGetMySavedAlbums';

describe('Testing useGetMySavedAlbums', () => {
  it('should be defined', () => {
    expect(useGetMySavedAlbums).toBeDefined();
  });
});
